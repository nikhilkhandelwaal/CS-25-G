#include<stdio.h>

int main() {

    int a = 10, b = 15, c = 20;
    if (a * a == (b * b) + (c * c)) {

    }
    else if (b * b == (a * a) + (c * c)) {

    }
    else if (c * c == (a * a) + (b * b)) {

    }
    else {
        
    }


    return 0;
}